---
const services = [
  {
    title: 'Structured Cabling',
    description: 'Ethernet runs, patch panels, and wall jacks — the wired backbone that gives your Wi-Fi something solid to build on.',
  },
  {
    title: 'Wi-Fi Access Points',
    description: 'Proper access points placed where they need to be for full coverage. No more dead zones, no more range extenders.',
  },
  {
    title: 'Network Segmentation',
    description: 'Separate networks for your smart home devices, guest access, and personal devices — so they\'re not all mixed together.',
  },
  {
    title: 'Camera Systems',
    description: 'Security cameras with local storage. View remotely when you need to, no monthly cloud subscription required.',
  },
  {
    title: 'Firewalls & Switches',
    description: 'Dedicated hardware for routing and switching — replacing the all-in-one box your ISP gave you with gear that actually performs.',
  },
  {
    title: 'Ongoing Support',
    description: 'Optional remote monitoring, firmware updates, and tweaks as your setup evolves. I don\'t disappear after install.',
  },
];
---

<section id="services" class="py-24 px-6 bg-white/[0.02]">
  <div class="max-w-6xl mx-auto">
    <h2 class="text-3xl md:text-4xl font-bold text-white mb-4 text-center font-heading">Services</h2>
    <p class="text-gray-400 text-center mb-16 max-w-xl mx-auto">Everything you need to replace that ISP box with a network that actually works.</p>
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {services.map(s => (
        <div class="bg-white/[0.03] border border-white/[0.06] rounded-2xl p-8">
          <h3 class="text-lg font-semibold text-white mb-3 font-heading">{s.title}</h3>
          <p class="text-gray-400 leading-relaxed">{s.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
